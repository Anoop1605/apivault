package com.sentinel.forensics.config;

import org.springframework.context.annotation.Configuration;

/**
 * OpenAPI Configuration — Swagger documentation disabled (springdoc-openapi not available in this service).
 * Can be re-enabled when Swagger dependency is added to pom.xml.
 */
@Configuration
public class OpenApiConfig {
    // Configuration intentionally empty to avoid Swagger dependency issues
}
