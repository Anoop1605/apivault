spring.application.name=forensics-replay-service
server.port=8083

# Disable DB auto-configuration (forensics service uses no DB — reads from event-store)
spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.jdbc.DataSourceAutoConfiguration,\
  org.springframework.boot.autoconfigure.orm.jpa.HibernateJpaAutoConfiguration,\
  org.springframework.boot.autoconfigure.data.jpa.JpaRepositoriesAutoConfiguration

# Actuator
management.endpoints.web.exposure.include=health,info,prometheus
management.endpoint.health.show-details=always

# Event Store integration
# Set use-mock=false when event-store-service is running with a real DB
event-store.use-mock=false
event-store.base-url=${EVENT_STORE_URL:http://localhost:8081}

# Also support the docker-compose env var EVENTSTORE_BASE_URL
eventstore.base-url=${EVENTSTORE_BASE_URL:${event-store.base-url}}

# Policy Engine integration
policy-engine.base-url=${POLICY_ENGINE_URL:http://localhost:8082}

# Security - disable for local dev
spring.security.user.name=admin
spring.security.user.password=admin
spring.main.allow-bean-definition-overriding=true
