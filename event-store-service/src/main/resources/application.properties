<<<<<<< HEAD
spring.application.name=event-store-service
server.port=8082

# PostgreSQL - fill in real values when DB is available
spring.datasource.url=${DB_URL:jdbc:postgresql://localhost:5432/sentinel_events}
spring.datasource.username=${DB_USER:sentinel}
spring.datasource.password=${DB_PASS:sentinel}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Flyway
spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration

# Actuator
management.endpoints.web.exposure.include=health,info,prometheus
management.endpoint.health.show-details=always

# Security
spring.security.user.name=admin
spring.security.user.password=admin
=======
spring.datasource.url=jdbc:postgresql://localhost:5432/apivault
spring.datasource.username=postgres
spring.datasource.password=postgres

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

spring.flyway.enabled=false
>>>>>>> 7ee7b36 (feat(event-store):add hash chain + forensic APIs)
